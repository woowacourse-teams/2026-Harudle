services:
  backend:
    logging: !override
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"

  frontend:
    logging: !override
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
