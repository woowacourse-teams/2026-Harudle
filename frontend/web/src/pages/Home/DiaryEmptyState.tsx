import ActionButton from '../../shared/ActionButton';

const DiaryEmptyState = () => {
  return (
    <div>
      <div>아직 기록이 없어요.</div>
      <ActionButton />
    </div>
  );
};

export default DiaryEmptyState;
